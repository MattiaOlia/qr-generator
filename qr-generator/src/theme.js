import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
      primary: {
        main: "#212121",
        contrastText: "	#F9F6EE",
      },
      secondary: {
        main: "#0277bd",
      },
      
      backgroundWhite: {
        main: "#fdfdfd",
      },
    },
  });

export default theme